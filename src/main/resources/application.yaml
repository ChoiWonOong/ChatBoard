spring:
  datasource:
    url: "jdbc:mysql://localhost/board"
    username: "root"
    password: "dnjsdnd1002!"
    driver-class-name: com.mysql.cj.jdbc.Driver
---
spring:
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        show_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect
---
spring:
  devtools:
    livereload:
      enabled: true
    restart:
      enabled: false
  freemarker:
    cache: false
